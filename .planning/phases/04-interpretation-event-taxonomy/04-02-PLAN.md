---
phase: 04-interpretation-event-taxonomy
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/sva/interpret/adapters/claude.py
  - src/sva/interpret/prompt.py
  - src/sva/observability/langfuse.py
  - tests/test_interpret_adapter.py
  - tests/test_observability.py
autonomous: true
requirements:
  - INTERPRET-01
  - INTERPRET-02
  - INTERPRET-03
  - OBS-02
  - EVENT-04
  - EVENT-06
  - EVENT-07
must_haves:
  truths:
    - "Claude Sonnet 4.5 becomes the real default interpreter and emits canonical Event[] rather than the Phase 1 stub."
    - "Prompt composition keeps observations, rule data, and retrieved memory as separate explicit inputs while Phase 5 retrieval remains compatible."
    - "Interpret traces carry prompt-version identity and clear failure/validation surfaces."
    - "Best-effort classifications degrade to `unknown` rather than hallucinated confidence."
  artifacts:
    - path: "src/sva/interpret/adapters/claude.py"
      provides: "real Claude interpretation adapter with structured Event[] parsing"
    - path: "src/sva/interpret/prompt.py"
      provides: "prompt builder that composes observations, rules, and memory"
    - path: "tests/test_interpret_adapter.py"
      provides: "adapter parsing and fallback coverage for multi-event interpretation"
  key_links:
    - from: "src/sva/interpret/prompt.py"
      to: "src/sva/interpret/rules.py"
      via: "prompt builder renders rule-data summary and rule refs from canonical rules loader"
      pattern: "rules"
    - from: "src/sva/interpret/adapters/claude.py"
      to: "src/sva/observability/langfuse.py"
      via: "interpret traces emit prompt-version identity and terminal status through TraceContext"
      pattern: "prompt_version_hash"
    - from: "src/sva/interpret/adapters/claude.py"
      to: "src/sva/memory/retriever.py"
      via: "adapter preserves retrieved-memory seam even when retrieval is []"
      pattern: "retrieved"
---

<objective>
Replace the Phase 1 Claude stub with a real, structured-output interpretation adapter that consumes point-scoped observations plus rules data, emits canonical Event[] with honest fallbacks, and keeps the interpret observability/auditability contract explicit.

Purpose: once the event contract and validator backbone exist, Phase 4 needs the real LLM path that proposes events from observations and rules rather than returning one placeholder row.

Output: `ClaudeInterpreter` becomes a real provider adapter with explicit prompt composition, canonical Event[] parsing, prompt-version identity, and surfaced validation/fallback behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-interpretation-event-taxonomy/04-CONTEXT.md
@.planning/phases/04-interpretation-event-taxonomy/04-RESEARCH.md
@.planning/phases/04-interpretation-event-taxonomy/04-PATTERNS.md
@src/sva/interpret/adapters/claude.py
@src/sva/interpret/runner.py
@src/sva/interpret/rules.py
@src/sva/memory/retriever.py
@src/sva/observability/langfuse.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Implement the real Claude interpretation path and prompt builder</name>
  <files>src/sva/interpret/adapters/claude.py, src/sva/interpret/prompt.py, tests/test_interpret_adapter.py</files>
  <read_first>
    - src/sva/interpret/adapters/claude.py
    - src/sva/interpret/runner.py
    - src/sva/memory/retriever.py
    - src/sva/models.py
    - .planning/research/STACK.md
    - .planning/phases/04-interpretation-event-taxonomy/04-CONTEXT.md
  </read_first>
  <action>
Replace the stubbed Claude adapter with a real structured-output call path that emits canonical `list[Event]`. Introduce a prompt-builder helper that composes: point-scoped observations, USAU rule summary, and retrieved memory examples/rules without collapsing those seams together. Keep the adapter behind `make_default_interpreter()` and parse provider output into canonical event rows only. Honor D-08 and D-11 by degrading uncertain turnover subtype, pass direction, and throw type to `"unknown"` instead of inventing precision. Expand `tests/test_interpret_adapter.py` so mocked provider responses prove multi-event parsing, best-effort fallback behavior, and compatibility with empty retrieval.
  </action>
  <acceptance_criteria>
    - `ClaudeInterpreter` performs a real structured interpretation path instead of returning one placeholder event
    - prompt composition is factored into a dedicated helper and keeps observations/rules/memory explicit
    - adapter output is canonical `list[Event]` only, with no provider payload leakage past canonical fields
    - `tests/test_interpret_adapter.py` covers multi-event parsing and best-effort unknown fallbacks
    - `./.venv/bin/pytest tests/test_interpret_adapter.py -q` passes
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Preserve interpret observability and surface validation/failure paths</name>
  <files>src/sva/interpret/adapters/claude.py, src/sva/observability/langfuse.py, tests/test_interpret_adapter.py, tests/test_observability.py</files>
  <read_first>
    - src/sva/observability/langfuse.py
    - src/sva/interpret/adapters/claude.py
    - .planning/research/PITFALLS.md
    - .planning/phases/04-interpretation-event-taxonomy/04-RESEARCH.md
  </read_first>
  <action>
Extend the interpret path so prompt-version identity, validation failures, and terminal failure status remain visible through the existing observability contract. Reuse the `TraceContext`/`observe_call(...)` pattern already established in Phases 1 and 3 rather than inventing interpret-only logging. The adapter should surface structured invalid-output and retry/fallback behavior explicitly so debugging later memory/rule changes is possible. Expand tests so cache-free interpret reruns still preserve prompt-hash sensitivity and failure-path trace updates.
  </action>
  <acceptance_criteria>
    - interpret traces expose prompt-version identity and explicit validation/failure status
    - invalid structured output does not disappear silently; it surfaces through a dedicated path the runner can reason about later
    - observability behavior remains aligned with the existing `observe_call(...)` contract
    - `tests/test_observability.py` and adapter tests cover the interpret observability path
    - `./.venv/bin/pytest tests/test_interpret_adapter.py tests/test_observability.py -q` passes
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_interpret_adapter.py tests/test_observability.py -q`
- `./.venv/bin/python -m py_compile src/sva/interpret/adapters/claude.py src/sva/interpret/prompt.py`
</verification>
