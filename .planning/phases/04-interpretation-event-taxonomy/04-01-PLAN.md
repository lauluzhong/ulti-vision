---
phase: 04-interpretation-event-taxonomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sva/models.py
  - src/sva/interpret/adapters/base.py
  - src/sva/interpret/runner.py
  - src/sva/interpret/rules.py
  - rulebook/usau_2024_2025.yaml
  - tests/test_models.py
  - tests/test_interpret_rules.py
autonomous: true
requirements:
  - INTERPRET-01
  - INTERPRET-02
  - INTERPRET-03
  - EVENT-01
  - EVENT-02
  - EVENT-03
  - EVENT-04
  - EVENT-06
  - EVENT-07
must_haves:
  truths:
    - "The interpret seam returns canonical `Event[]` per point rather than compressing a whole point into one synthetic event."
    - "USAU rules are loaded from repo data files instead of being hardcoded inside prompt strings."
    - "A deterministic validation layer exists for high-value state contradictions and fails open via annotations rather than silent drops."
    - "The canonical Event contract preserves auditability through explicit source/rule/memory provenance and prompt-version identity."
  artifacts:
    - path: "src/sva/models.py"
      provides: "canonical Event schema widened for Phase 4 auditability and best-effort detail fields"
    - path: "src/sva/interpret/adapters/base.py"
      provides: "swap-safe interpreter protocol returning Event[]"
    - path: "src/sva/interpret/runner.py"
      provides: "point-level interpret orchestration seam returning Event[]"
    - path: "src/sva/interpret/rules.py"
      provides: "rule-data loader and deterministic validator backbone"
    - path: "rulebook/usau_2024_2025.yaml"
      provides: "repo-visible canonical USAU alpha rules data"
  key_links:
    - from: "rulebook/usau_2024_2025.yaml"
      to: "src/sva/interpret/rules.py"
      via: "rules load from data rather than code constants"
      pattern: "USAU"
    - from: "src/sva/interpret/adapters/base.py"
      to: "src/sva/interpret/runner.py"
      via: "runner widens to Event[] without breaking swap-safe injection"
      pattern: "list[Event]"
    - from: "src/sva/models.py"
      to: "src/sva/interpret/rules.py"
      via: "validator reasons over canonical Event objects, not provider payloads"
      pattern: "Event"
---

<objective>
Create the Phase 4 contract backbone: widen interpretation to `Event[]`, introduce USAU rules as repo data, and land a minimal deterministic validator that can catch the highest-value timeline contradictions before the real Claude path ships.

Purpose: Phase 4 cannot be planned around a one-event stub or prompt-only rules. This plan establishes the real contract and rule-data surface that later provider and pipeline work depend on.

Output: the repo has a repo-visible USAU rule-data file, a deterministic validator backbone, and a swap-safe interpreter boundary that truthfully returns canonical `Event[]` per point.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-interpretation-event-taxonomy/04-CONTEXT.md
@.planning/phases/04-interpretation-event-taxonomy/04-RESEARCH.md
@.planning/phases/04-interpretation-event-taxonomy/04-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/ARCHITECTURE.md
@src/sva/models.py
@src/sva/interpret/adapters/base.py
@src/sva/interpret/runner.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Widen the canonical interpret seam and event contract</name>
  <files>src/sva/models.py, src/sva/interpret/adapters/base.py, src/sva/interpret/runner.py, tests/test_models.py, tests/test_interpret_adapter.py</files>
  <read_first>
    - src/sva/models.py
    - src/sva/interpret/adapters/base.py
    - src/sva/interpret/runner.py
    - .planning/phases/04-interpretation-event-taxonomy/04-CONTEXT.md
    - .planning/phases/04-interpretation-event-taxonomy/04-RESEARCH.md
  </read_first>
  <action>
Widen the interpreter contract from a single `Event` to `list[Event]` per D-01 and preserve swap-safety while doing it. Update the canonical `Event` model only where Phase 4 auditability and best-effort classification need more explicit structure: preserve `source_observations`, `rule_refs`, `memory_refs`, and warnings, and add a canonical interpretation prompt-version identity plus any strongly-typed detail fields needed for turnover subtype, throw type, and pass direction. Keep the closed event enum narrow and alpha-focused rather than adding every possible Ultimate-specific call. Update the runner and the existing interpreter test seam so the repo’s public interpret boundary now truthfully represents a point timeline rather than a one-event placeholder.
  </action>
  <acceptance_criteria>
    - the `Interpreter` protocol and `run_point(...)` return canonical `list[Event]`
    - `Event` remains model-agnostic and audit-friendly, with explicit prompt/rule/source provenance available on emitted rows
    - the closed enum stays aligned with the roadmap’s v1 event set rather than expanding into low-value stoppage calls
    - `tests/test_models.py` and `tests/test_interpret_adapter.py` are updated for the widened contract
    - `./.venv/bin/pytest tests/test_models.py tests/test_interpret_adapter.py -q` passes
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Introduce USAU rules-as-data and the deterministic validator backbone</name>
  <files>rulebook/usau_2024_2025.yaml, src/sva/interpret/rules.py, tests/test_interpret_rules.py</files>
  <read_first>
    - .planning/research/FEATURES.md
    - .planning/research/ARCHITECTURE.md
    - .planning/research/PITFALLS.md
    - .planning/phases/04-interpretation-event-taxonomy/04-CONTEXT.md
    - src/sva/models.py
  </read_first>
  <action>
Create a repo-visible USAU alpha rules data file and a thin interpretation-side loader/validator module. The loader should expose a prompt-friendly summary plus structured rule references; the validator should focus on the highest-value deterministic contradictions only: impossible possession flips, goal/team contradictions, and point-end sequencing violations. Keep the validator fail-open per D-06 by returning structured warnings/violations rather than deleting events silently. Add focused tests proving rules load from data and that the validator catches the intended contradictions without pretending to cover the whole rulebook.
  </action>
  <acceptance_criteria>
    - Phase 4 introduces a canonical USAU rules data file in the repo
    - the validator works over canonical `Event` objects instead of provider payloads
    - hard contradictions surface as structured validation findings rather than silent drops
    - `tests/test_interpret_rules.py` proves both rules loading and core contradiction detection
    - `./.venv/bin/pytest tests/test_interpret_rules.py -q` passes
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_models.py tests/test_interpret_adapter.py tests/test_interpret_rules.py -q`
- `./.venv/bin/python -m py_compile src/sva/models.py src/sva/interpret/adapters/base.py src/sva/interpret/runner.py src/sva/interpret/rules.py`
</verification>
