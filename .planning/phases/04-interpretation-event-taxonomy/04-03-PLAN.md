---
phase: 04-interpretation-event-taxonomy
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/sva/pipeline.py
  - src/sva/events_dao.py
  - tests/test_point_scoped_pipeline.py
  - tests/test_events_dao.py
  - tests/test_cli_e2e.py
autonomous: true
requirements:
  - EVENT-01
  - EVENT-02
  - EVENT-03
  - EVENT-04
  - EVENT-05
  - EVENT-06
  - EVENT-07
  - EVENT-08
  - INTERPRET-01
  - INTERPRET-03
must_haves:
  truths:
    - "The point-aware pipeline persists multiple canonical events per point rather than one synthetic point summary event."
    - "Event persistence remains sliceable by point, event type, and team after interpret widens to Event[]."
    - "Pass count is queryable as a derived per-point aggregate from canonical events rather than a fake event type."
    - "Pipeline integration preserves point ownership and fan-out ordering from detect -> observe -> interpret -> persist."
  artifacts:
    - path: "src/sva/pipeline.py"
      provides: "multi-event point fan-out integration"
    - path: "src/sva/events_dao.py"
      provides: "event persistence helpers suitable for multi-row writes and point/event filters"
    - path: "tests/test_point_scoped_pipeline.py"
      provides: "integration proof that point-aware pipeline now persists multi-event timelines"
    - path: "tests/test_events_dao.py"
      provides: "DB-gated event query/persistence coverage for the widened event contract"
  key_links:
    - from: "src/sva/pipeline.py"
      to: "src/sva/interpret/runner.py"
      via: "pipeline consumes Event[] per point and fans out inserts deterministically"
      pattern: "run_point"
    - from: "src/sva/pipeline.py"
      to: "src/sva/events_dao.py"
      via: "pipeline persists canonical multi-event rows and keeps point/team/type querying intact"
      pattern: "insert_event"
    - from: "src/sva/events_dao.py"
      to: "src/sva/models.py"
      via: "DAO writes canonical audit fields including rule/source/memory provenance"
      pattern: "rule_refs"
---

<objective>
Integrate the widened interpretation layer into the real point-aware pipeline: persist multiple canonical events per point, keep point/team/type queryability intact, and prove the v1 event timeline now exists as first-class data instead of a single placeholder row.

Purpose: Phase 4 is only real if the pipeline and DB can carry the multi-event timeline that the adapter emits.

Output: point-scoped pipeline runs now persist multi-row event timelines, event queries remain point-aware, and tests prove the timeline fan-out is stable and auditable.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-interpretation-event-taxonomy/04-CONTEXT.md
@.planning/phases/04-interpretation-event-taxonomy/04-RESEARCH.md
@.planning/phases/04-interpretation-event-taxonomy/04-PATTERNS.md
@.planning/phases/03-perception-layer/03-03-SUMMARY.md
@src/sva/pipeline.py
@src/sva/events_dao.py
@src/sva/interpret/runner.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Fan out multiple interpreted events through the point-aware pipeline</name>
  <files>src/sva/pipeline.py, tests/test_point_scoped_pipeline.py, tests/test_cli_e2e.py</files>
  <read_first>
    - src/sva/pipeline.py
    - src/sva/interpret/runner.py
    - .planning/phases/03-perception-layer/03-03-SUMMARY.md
    - .planning/phases/04-interpretation-event-taxonomy/04-CONTEXT.md
  </read_first>
  <action>
Update `src/sva/pipeline.py` so `run_point(...)` returning `list[Event]` becomes the new normal. Preserve the existing point-aware grouping and observation persistence order from Phase 3, but fan out canonical events for each point and apply point scope consistently before persistence. Extend the pipeline tests so one point can produce multiple event rows in deterministic order and the CLI smoke path still works with the widened interpretation surface.
  </action>
  <acceptance_criteria>
    - the pipeline persists multiple canonical events per point rather than one summary row
    - point ownership still comes from persisted Phase 2 point rows, not re-derived interpret logic
    - `tests/test_point_scoped_pipeline.py` proves multi-event fan-out ordering and point scoping
    - any affected CLI/integration test remains green or explicitly updated for the widened return shape
    - `./.venv/bin/pytest tests/test_point_scoped_pipeline.py tests/test_cli_e2e.py -q` passes
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Preserve event queryability and derived per-point aggregates</name>
  <files>src/sva/events_dao.py, tests/test_events_dao.py</files>
  <read_first>
    - src/sva/events_dao.py
    - tests/test_events_dao.py
    - src/sva/models.py
    - .planning/ROADMAP.md
    - .planning/phases/04-interpretation-event-taxonomy/04-RESEARCH.md
  </read_first>
  <action>
Extend the event persistence seam so the widened event contract remains easy to query by point, event type, and team per `EVENT-08`, and so pass count per point can be derived cleanly from canonical event rows per `EVENT-05`. This may include a batch insert helper and/or richer point/event query helpers, but should stay within the repo’s existing narrow-DAO pattern. Expand DB-gated event DAO coverage accordingly, especially for point filtering, multi-event ordering, and any new canonical audit fields introduced earlier in the phase.
  </action>
  <acceptance_criteria>
    - event persistence remains point-aware and queryable by point and event type
    - pass count can be derived from canonical stored events without inventing a `pass_count` event type
    - DB-gated tests cover the widened event contract and point/event filtering
    - `./.venv/bin/pytest tests/test_events_dao.py -q` passes or skips only when Postgres is unavailable
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_point_scoped_pipeline.py tests/test_events_dao.py tests/test_cli_e2e.py -q`
- `./.venv/bin/python -m py_compile src/sva/pipeline.py src/sva/events_dao.py`
</verification>
