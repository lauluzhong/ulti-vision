---
phase: 06-async-orchestration-and-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sva/pipeline.py
  - src/sva/ingest/ingest.py
  - src/sva/jobs_dao.py
  - src/sva/jobs_service.py
  - migrations/versions/0009_phase6_job_progress.py
  - tests/test_db_migration.py
  - tests/test_jobs_service.py
autonomous: true
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - "The repo has a durable job lifecycle and explicit progress truth instead of a purely synchronous pipeline call."
    - "Resume behavior is driven by persisted state that already exists in Postgres rather than transient in-memory worker flags."
    - "Queue-specific integration stays thin around normal Python orchestration services."
  artifacts:
    - path: "src/sva/jobs_dao.py"
      provides: "canonical job progress persistence helpers"
    - path: "src/sva/jobs_service.py"
      provides: "durable orchestration and resume helpers"
    - path: "migrations/versions/0009_phase6_job_progress.py"
      provides: "job progress schema support"
    - path: "tests/test_jobs_service.py"
      provides: "service-level proof of stage progression and resume behavior"
  key_links:
    - from: "src/sva/jobs_service.py"
      to: "src/sva/pipeline.py"
      via: "stage-aware orchestration reuses canonical pipeline boundaries instead of duplicating them in routes"
      pattern: "run_pipeline"
    - from: "src/sva/jobs_service.py"
      to: "src/sva/observations_dao.py"
      via: "resume logic inspects persisted artifacts before re-running perception"
      pattern: "list_cached_observations"
    - from: "migrations/versions/0009_phase6_job_progress.py"
      to: "tests/test_db_migration.py"
      via: "migration smoke suite asserts the new job-progress surface"
      pattern: "jobs"
---

<objective>
Create the durable Phase 6 orchestration substrate: explicit job-progress persistence, resume-safe stage helpers, and a queue-agnostic service layer that later HTTP routes and worker wrappers can call.

Purpose: async HTTP submission is not honest until the repo has durable job-state truth and resumable orchestration behind it.

Output: the repo can submit and track jobs through persisted lifecycle state, and orchestration can resume from already-persisted artifacts instead of re-running everything blindly.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-async-orchestration-and-api/06-CONTEXT.md
@.planning/phases/06-async-orchestration-and-api/06-RESEARCH.md
@.planning/phases/06-async-orchestration-and-api/06-PATTERNS.md
@src/sva/pipeline.py
@src/sva/ingest/ingest.py
@src/sva/observations_dao.py
@src/sva/events_dao.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add canonical job-progress persistence and lifecycle helpers</name>
  <files>src/sva/ingest/ingest.py, src/sva/jobs_dao.py, migrations/versions/0009_phase6_job_progress.py, tests/test_db_migration.py</files>
  <read_first>
    - src/sva/ingest/ingest.py
    - tests/test_db_migration.py
    - .planning/phases/06-async-orchestration-and-api/06-CONTEXT.md
    - .planning/phases/06-async-orchestration-and-api/06-RESEARCH.md
  </read_first>
  <action>
Create a narrow persistence surface for job lifecycle and progress. Extend or normalize the existing `jobs` truth so Phase 6 can report stage status, partial completion, and terminal state without inventing a second competing job store. Keep the DAO small and explicit.
  </action>
  <acceptance_criteria>
    - job lifecycle/progress truth is persisted and queryable
    - the migration cleanly extends the existing Postgres-first architecture
    - DB smoke coverage reflects the new surface
    - `./.venv/bin/pytest tests/test_db_migration.py -q` passes or skips only when Postgres is unavailable
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Factor the synchronous pipeline into resume-safe orchestration services</name>
  <files>src/sva/pipeline.py, src/sva/jobs_service.py, tests/test_jobs_service.py</files>
  <read_first>
    - src/sva/pipeline.py
    - src/sva/observations_dao.py
    - src/sva/events_dao.py
    - .planning/phases/06-async-orchestration-and-api/06-PATTERNS.md
  </read_first>
  <action>
Refactor the current end-to-end pipeline into explicit stage helpers that can be called from a worker or route submission path. Resume logic must inspect persisted truth first and skip work that is already durable, especially point detection, cached observations, and already-persisted point/event slices.
  </action>
  <acceptance_criteria>
    - orchestration logic exists outside routes and can be called independently
    - resume behavior uses persisted state instead of transient flags alone
    - focused service tests prove stage progression and skip/continue behavior
    - `./.venv/bin/pytest tests/test_jobs_service.py tests/test_point_scoped_pipeline.py -q` passes
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_jobs_service.py tests/test_point_scoped_pipeline.py tests/test_db_migration.py -q`
- `./.venv/bin/python -m py_compile src/sva/pipeline.py src/sva/ingest/ingest.py src/sva/jobs_dao.py src/sva/jobs_service.py migrations/versions/0009_phase6_job_progress.py`
</verification>
