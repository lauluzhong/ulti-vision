---
phase: 06-async-orchestration-and-api
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/sva/api/app.py
  - src/sva/api/schemas.py
  - src/sva/jobs_service.py
  - tests/test_api_app.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
must_haves:
  truths:
    - "The FastAPI surface submits jobs asynchronously and returns job metadata immediately instead of blocking on full pipeline completion."
    - "Job status and event-read routes are thin wrappers over canonical service/DAO seams."
    - "The API exposes partial progress and canonical event filtering without re-running pipeline logic."
  artifacts:
    - path: "src/sva/api/app.py"
      provides: "async job submission and status/event routes"
    - path: "src/sva/api/schemas.py"
      provides: "stable route response/request shapes"
    - path: "tests/test_api_app.py"
      provides: "route-level proof for submission, status, and event reads"
  key_links:
    - from: "src/sva/api/app.py"
      to: "src/sva/jobs_service.py"
      via: "routes delegate submission and status reads to durable services"
      pattern: "submit"
    - from: "src/sva/api/app.py"
      to: "src/sva/events_dao.py"
      via: "event timeline reads stay canonical and filterable"
      pattern: "event_type"
---

<objective>
Upgrade the HTTP surface from synchronous ingest normalization to async job submission and status/event reads backed by the Phase 6 orchestration substrate.

Purpose: Phase 6 is only useful to clients if they can submit a game, poll progress, and read canonical stored results without blocking on the whole pipeline.

Output: `POST /ingest`, `GET /jobs/:id`, and `GET /games/:id/events` are live and route through stable service/DAO seams.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-async-orchestration-and-api/06-CONTEXT.md
@.planning/phases/06-async-orchestration-and-api/06-RESEARCH.md
@.planning/phases/06-async-orchestration-and-api/06-PATTERNS.md
@src/sva/api/app.py
@src/sva/jobs_service.py
@src/sva/events_dao.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Convert ingest submission into async job creation</name>
  <files>src/sva/api/app.py, src/sva/api/schemas.py, tests/test_api_app.py</files>
  <read_first>
    - src/sva/api/app.py
    - .planning/phases/06-async-orchestration-and-api/06-CONTEXT.md
    - .planning/phases/06-async-orchestration-and-api/06-RESEARCH.md
  </read_first>
  <action>
Replace the current synchronous `/ingest` behavior with async submission semantics that validate the request, persist/submit a job, and return job metadata immediately. Preserve the current file-vs-url validation rules and rights-ack behavior for remote ingest.
  </action>
  <acceptance_criteria>
    - `POST /ingest` returns a job id immediately instead of full pipeline output
    - file and URL sources still validate exactly-one-source semantics
    - submission tests cover success and request-shape failures
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Expose job-status and canonical event-read routes</name>
  <files>src/sva/api/app.py, src/sva/jobs_service.py, tests/test_api_app.py</files>
  <read_first>
    - src/sva/events_dao.py
    - src/sva/jobs_service.py
    - tests/test_events_dao.py
  </read_first>
  <action>
Add `GET /jobs/:id` and `GET /games/:id/events` using the durable job-progress and canonical event persistence seams. Status responses should expose stage-level progress and partial completion state; event reads should support filtering by point, event type, and team.
  </action>
  <acceptance_criteria>
    - job-status route returns stage/progress truth from persisted services
    - event-read route returns canonical stored events with point/type/team filters
    - API tests cover both routes and key failure paths
    - `./.venv/bin/pytest tests/test_api_app.py -q` passes
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_api_app.py tests/test_events_dao.py -q`
- `./.venv/bin/python -m py_compile src/sva/api/app.py src/sva/api/schemas.py src/sva/jobs_service.py`
</verification>
