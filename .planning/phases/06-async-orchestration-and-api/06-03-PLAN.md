---
phase: 06-async-orchestration-and-api
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/sva/api/app.py
  - src/sva/api/contracts.py
  - src/sva/memory/service.py
  - tests/test_events_api.py
  - tests/test_corrections_api.py
autonomous: true
requirements:
  - API-03
  - API-04
must_haves:
  truths:
    - "`GET /games/:id/events` returns canonical stored events and can filter by point, event type, and team without re-running interpretation."
    - "`POST /games/:id/corrections` appends immutable correction rows and triggers the existing correction-to-memory path rather than mutating events in place."
    - "The public correction write-path remains thin and coach-provenance aware even without auth in Phase 6."
  artifacts:
    - path: "src/sva/api/contracts.py"
      provides: "stable request/response models for events and corrections routes"
    - path: "src/sva/memory/service.py"
      provides: "thin correction orchestration over existing DAO and memory-writer seams"
    - path: "tests/test_events_api.py"
      provides: "canonical event-read route coverage"
    - path: "tests/test_corrections_api.py"
      provides: "immutable correction and memory-write route coverage"
  key_links:
    - from: "src/sva/api/app.py"
      to: "src/sva/events_dao.py"
      via: "event route reads canonical persisted rows only"
      pattern: "list_event_rows"
    - from: "src/sva/api/app.py"
      to: "src/sva/memory/service.py"
      via: "correction route delegates into the Phase 5 correction + memory substrate"
      pattern: "correction"
    - from: "src/sva/memory/service.py"
      to: "src/sva/memory/writer.py"
      via: "HTTP writes produce coach-scoped derived memory rows through existing logic"
      pattern: "correction_to_memory_records"
---

<objective>
Expose the canonical event-read and correction-write surfaces on top of the existing Phase 4 and Phase 5 storage contracts.

Purpose: async submission/status alone is not enough; clients and later UI work need a stable event timeline read surface plus a real correction write-path that compounds into memory safely.

Output: `GET /games/:id/events`, `POST /games/:id/corrections`, typed API contracts, and a thin memory service that preserves the existing immutable correction pipeline.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-async-orchestration-and-api/06-CONTEXT.md
@.planning/phases/06-async-orchestration-and-api/06-RESEARCH.md
@.planning/phases/06-async-orchestration-and-api/06-PATTERNS.md
@.planning/phases/04-interpretation-event-taxonomy/04-03-SUMMARY.md
@.planning/phases/05-memory-correction-loop/05-04-SUMMARY.md
@src/sva/api/app.py
@src/sva/events_dao.py
@src/sva/memory/corrections_dao.py
@src/sva/memory/writer.py
@src/sva/memory/records_dao.py

<interfaces>
From `src/sva/events_dao.py`:

```python
def list_event_rows(game_id: str, *, point_id: str | None = None, event_type: str | None = None, team: str | None = None) -> list[EventRow]:
    ...
```

From `src/sva/memory/writer.py`:

```python
def correction_to_memory_records(correction: CorrectionRecord, *, created_at: datetime | None = None) -> list[MemoryRecord]:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical event-read filtering over persisted rows</name>
  <files>src/sva/api/app.py, src/sva/api/contracts.py, tests/test_events_api.py</files>
  <behavior>
    - Test 1: `GET /games/{game_id}/events` returns stored rows ordered by time.
    - Test 2: filters by point, event type, and team narrow the canonical dataset without recomputation.
    - Test 3: missing games return a clean not-found or empty canonical response, not synthetic events.
  </behavior>
  <action>
Implement D-05 and D-08 by adding `GET /games/{game_id}/events` as a thin wrapper over the existing canonical DAO seam. Keep the route in `src/sva/api/app.py` per the current repo pattern, define stable serializers in `src/sva/api/contracts.py`, and do not re-run interpretation or compute alternate event truth on the fly.
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_events_api.py -q</automated>
  </verify>
  <done>The event-read route serves canonical stored events only, supports the required filters, and stays a thin serializer over existing persistence helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add public corrections submission over the Phase 5 memory substrate</name>
  <files>src/sva/api/app.py, src/sva/api/contracts.py, src/sva/memory/service.py, tests/test_corrections_api.py</files>
  <behavior>
    - Test 1: `POST /games/{game_id}/corrections` persists an immutable correction row and derives coach-scoped memory rows.
    - Test 2: the route records `coach_id` provenance even though auth is deferred.
    - Test 3: invalid correction payloads fail fast without mutating events or memory.
  </behavior>
  <action>
Implement D-09 and D-11 by adding `POST /games/{game_id}/corrections` as a thin adapter over the existing immutable correction + memory-writer path. Create `src/sva/memory/service.py` only as a small orchestration seam that persists the correction row first, then derives/persists memory rows from that snapshot. Do not mutate `events` rows in place, do not invent a second correction model, and do not add auth scope in this phase.
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_corrections_api.py -q</automated>
  </verify>
  <done>Corrections are accepted via HTTP, remain append-only and coach-provenance aware, and flow into the existing memory substrate without redesigning Phase 5 contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> events/corrections routes | Untrusted callers request canonical game data and append coach feedback. |
| Correction route -> memory substrate | User-supplied corrections become persistent memory inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | T | `POST /games/{id}/corrections` | mitigate | Validate correction payloads against explicit request schemas and write immutable correction rows instead of mutating event rows in place. |
| T-06-03-02 | I | `GET /games/{id}/events` | mitigate | Serve only canonical event fields needed by the route contract and do not leak raw provider/debug payloads through the serializer. |
| T-06-03-03 | S | correction provenance | accept | Phase 6 intentionally omits auth per D-11; mitigate what is possible by requiring/storing `coach_id` on every correction and keeping the write path append-only for later audit. |
</threat_model>

<verification>
- `./.venv/bin/pytest tests/test_events_api.py tests/test_corrections_api.py -q`
- `./.venv/bin/python -m py_compile src/sva/api/app.py src/sva/api/contracts.py src/sva/memory/service.py`
</verification>

<success_criteria>
- Event reads are canonical and filterable.
- Corrections go through the existing immutable correction + memory path.
- No Phase 5 schema contracts are reopened.
</success_criteria>

<output>
After completion, create `.planning/phases/06-async-orchestration-and-api/06-03-SUMMARY.md`
</output>
