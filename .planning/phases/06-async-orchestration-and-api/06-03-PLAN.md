---
phase: 06-async-orchestration-and-api
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/sva/api/app.py
  - src/sva/export_csv.py
  - src/sva/memory/corrections_dao.py
  - src/sva/memory/writer.py
  - tests/test_api_app.py
  - tests/test_cli_e2e.py
  - tests/test_point_scoped_pipeline.py
autonomous: true
requirements:
  - API-04
  - API-05
  - EXPORT-01
must_haves:
  truths:
    - "The public correction route is a thin wrapper over the Phase 5 immutable correction and memory-writing path."
    - "CSV export is generated from canonical stored event rows with stable user-facing columns."
    - "Resume/crash-safety verification proves completed windows are not re-invoked on rerun."
  artifacts:
    - path: "src/sva/api/app.py"
      provides: "corrections and export routes"
    - path: "src/sva/export_csv.py"
      provides: "stable canonical CSV serialization"
    - path: "tests/test_api_app.py"
      provides: "route-level proof for corrections and export"
    - path: "tests/test_point_scoped_pipeline.py"
      provides: "resume-safe orchestration verification"
  key_links:
    - from: "src/sva/api/app.py"
      to: "src/sva/memory/writer.py"
      via: "correction route reuses the existing coach-safe memory writer path"
      pattern: "correction"
    - from: "src/sva/export_csv.py"
      to: "src/sva/events_dao.py"
      via: "CSV export serializes canonical stored events rather than recomputed output"
      pattern: "events"
---

<objective>
Finish the public Phase 6 surface with correction submission, CSV export, and explicit verification that resumed execution does not duplicate already-completed perception work.

Purpose: async submission and status are not enough unless clients can also correct results and export the canonical event timeline safely.

Output: `POST /games/:id/corrections` and `GET /exports/:game_id.csv` are live, and resume verification proves the durable orchestration story is honest.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-async-orchestration-and-api/06-CONTEXT.md
@.planning/phases/06-async-orchestration-and-api/06-RESEARCH.md
@.planning/phases/06-async-orchestration-and-api/06-PATTERNS.md
@src/sva/api/app.py
@src/sva/memory/corrections_dao.py
@src/sva/memory/writer.py
@src/sva/events_dao.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add public corrections submission on top of the Phase 5 substrate</name>
  <files>src/sva/api/app.py, tests/test_api_app.py</files>
  <read_first>
    - src/sva/memory/corrections_dao.py
    - src/sva/memory/writer.py
    - .planning/phases/05-memory-correction-loop/05-03-SUMMARY.md
  </read_first>
  <action>
Add `POST /games/:id/corrections` as a thin public wrapper over the immutable correction persistence and coach-safe memory writer path built in Phase 5. Keep correction semantics inside the memory package rather than reimplementing them in the route.
  </action>
  <acceptance_criteria>
    - correction route persists canonical immutable correction data
    - correction-derived memory rows stay coach-scoped by default
    - API tests prove the route delegates into the existing correction path
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Add canonical CSV export and resume-safe verification</name>
  <files>src/sva/export_csv.py, src/sva/api/app.py, tests/test_api_app.py, tests/test_cli_e2e.py, tests/test_point_scoped_pipeline.py</files>
  <read_first>
    - src/sva/events_dao.py
    - tests/test_point_scoped_pipeline.py
    - .planning/phases/03-perception-layer/03-03-SUMMARY.md
  </read_first>
  <action>
Serialize canonical stored events into a stable CSV export surface and expose it through `GET /exports/:game_id.csv`. Extend orchestration verification to prove that reruns after partial completion do not create duplicate perception work when cached observations already exist.
  </action>
  <acceptance_criteria>
    - CSV export uses canonical stored events and stable columns
    - export route returns a valid CSV response for a processed game
    - resume verification proves no duplicate perception work on rerun
    - `./.venv/bin/pytest tests/test_api_app.py tests/test_cli_e2e.py tests/test_point_scoped_pipeline.py -q` passes
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_api_app.py tests/test_cli_e2e.py tests/test_point_scoped_pipeline.py -q`
- `./.venv/bin/python -m py_compile src/sva/api/app.py src/sva/export_csv.py`
</verification>
