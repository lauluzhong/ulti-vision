---
phase: 06-async-orchestration-and-api
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - src/sva/api/app.py
  - src/sva/exports.py
  - tests/test_exports_api.py
  - tests/test_orchestration.py
  - tests/test_cli_e2e.py
autonomous: true
requirements:
  - API-05
  - EXPORT-01
must_haves:
  truths:
    - "`GET /exports/:game_id.csv` returns a stable versioned one-row-per-event CSV built from canonical stored events."
    - "User-facing CSV output omits internal ids and debug/cache fields."
    - "Resume-safety verification explicitly proves completed windows are not re-invoked after interruption and rerun."
  artifacts:
    - path: "src/sva/exports.py"
      provides: "versioned CSV column contract and serializer"
    - path: "tests/test_exports_api.py"
      provides: "route-level proof of CSV shape and headers"
    - path: "tests/test_orchestration.py"
      provides: "crash/resume regression coverage for completed-window reuse"
  key_links:
    - from: "src/sva/api/app.py"
      to: "src/sva/exports.py"
      via: "export route streams the single versioned CSV projection"
      pattern: "csv"
    - from: "src/sva/exports.py"
      to: "src/sva/events_dao.py"
      via: "CSV is generated from canonical persisted events rather than recomputed output"
      pattern: "list_event_rows"
    - from: "tests/test_orchestration.py"
      to: "src/sva/orchestration.py"
      via: "restart verification proves already-completed windows remain skipped"
      pattern: "resume"
---

<objective>
Finish Phase 6 with the stable CSV export contract and the final durability proof that restart does not duplicate completed work.

Purpose: the phase is not honestly done until exports are user-facing and the restart story is verified in the same repo that will serve clients.

Output: a versioned CSV serializer/route plus explicit crash-resume regression coverage for completed-window reuse.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-async-orchestration-and-api/06-CONTEXT.md
@.planning/phases/06-async-orchestration-and-api/06-RESEARCH.md
@.planning/phases/06-async-orchestration-and-api/06-PATTERNS.md
@.planning/research/PITFALLS.md
@src/sva/api/app.py
@src/sva/events_dao.py
@src/sva/orchestration.py
@tests/test_orchestration.py

<interfaces>
From `src/sva/events_dao.py`:

```python
def list_event_rows(game_id: str, *, point_id: str | None = None, event_type: str | None = None, team: str | None = None) -> list[EventRow]:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a versioned human-facing CSV export route</name>
  <files>src/sva/api/app.py, src/sva/exports.py, tests/test_exports_api.py</files>
  <behavior>
    - Test 1: `GET /exports/{game_id}.csv` returns `text/csv` and one row per canonical event.
    - Test 2: header order/version is stable and excludes internal ids like `event_id`, `source_observations`, `memory_refs`, and prompt hashes.
    - Test 3: exported rows stay sliceable by point and use user-facing column names.
  </behavior>
  <action>
Implement D-10 and `EXPORT-01` by creating `src/sva/exports.py` as the single versioned CSV projection helper, then expose it through `GET /exports/{game_id}.csv` in `src/sva/api/app.py`. Follow the research and pitfalls guidance: use canonical stored events only, freeze the column order in code, and keep the CSV human-facing rather than debug-facing.
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_exports_api.py -q</automated>
  </verify>
  <done>The export route returns a stable versioned CSV with documented user-facing columns and no internal ids/debug fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock in crash-resume verification for completed-window reuse</name>
  <files>tests/test_orchestration.py, tests/test_cli_e2e.py</files>
  <behavior>
    - Test 1: a simulated crash after some windows/points are committed resumes from the first unfinished unit only.
    - Test 2: rerun does not create duplicate completed-window perception work when cached observations already exist.
    - Test 3: the user-visible CLI/API happy path still reflects the durable resume behavior rather than the old one-shot assumption.
  </behavior>
  <action>
Close the remaining durability proof by extending orchestration and end-to-end regression tests around the Phase 6 resume contract from D-02. The verification should assert the boundary the repo can honestly guarantee: once an observation or point result is durably persisted, restart skips it. Do not weaken the claim into vague smoke coverage.
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_orchestration.py tests/test_cli_e2e.py -q</automated>
  </verify>
  <done>The repo has explicit regression coverage proving restart skips already-completed work, and the final export surface is locked to canonical stored data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> export route | Public callers receive a user-facing projection of internal event data. |
| Restart verification -> orchestration semantics | Weak tests can falsely claim durability that the code does not actually provide. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | I | CSV export | mitigate | Use a single projection helper that excludes internal ids, source observation refs, memory refs, and prompt/debug fields from user-facing CSV output. |
| T-06-04-02 | R | resume verification | mitigate | Assert the precise persisted checkpoint boundary in tests so the repo cannot claim crash-resume safety without evidence. |
| T-06-04-03 | D | export route | mitigate | Stream/read directly from canonical stored events and avoid heavyweight recomputation or whole-pipeline reruns during export requests. |
</threat_model>

<verification>
- `./.venv/bin/pytest tests/test_exports_api.py tests/test_orchestration.py tests/test_cli_e2e.py -q`
- `./.venv/bin/python -m py_compile src/sva/api/app.py src/sva/exports.py`
</verification>

<success_criteria>
- CSV export is stable, versioned, and user-facing.
- Restart safety is explicitly regression-tested, not implied.
- Phase 6 closes with both the API contract and the durability proof in place.
</success_criteria>

<output>
After completion, create `.planning/phases/06-async-orchestration-and-api/06-04-SUMMARY.md`
</output>
