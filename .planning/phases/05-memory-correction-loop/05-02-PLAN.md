---
phase: 05-memory-correction-loop
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/sva/memory/retriever.py
  - src/sva/memory/__init__.py
  - src/sva/interpret/prompt.py
  - src/sva/interpret/adapters/claude.py
  - tests/test_memory_retriever.py
  - tests/test_interpret_adapter.py
autonomous: true
requirements:
  - MEMORY-01
  - MEMORY-02
  - MEMORY-05
must_haves:
  truths:
    - "The retrieval stub is replaced with real scope-aware memory lookup while preserving the fixed public signature."
    - "Interpretation retrieves memory by event-type tag and bounded candidate ranking instead of blindly passing []."
    - "Retrieved memory ids survive into canonical event audit fields even when the LLM omits them."
    - "Embedding-provider concerns remain behind the memory seam rather than leaking into interpretation or event schemas."
  artifacts:
    - path: "src/sva/memory/retriever.py"
      provides: "scope-aware tag-first retrieval with bounded result count"
    - path: "src/sva/interpret/prompt.py"
      provides: "prompt composition still keeps retrieved memory explicit"
    - path: "tests/test_memory_retriever.py"
      provides: "retrieval behavior coverage beyond the Phase 1 stub"
  key_links:
    - from: "src/sva/memory/retriever.py"
      to: "src/sva/interpret/prompt.py"
      via: "retrieved memory stays an explicit prompt section"
      pattern: "Retrieved memory records"
    - from: "src/sva/memory/retriever.py"
      to: "src/sva/interpret/adapters/claude.py"
      via: "retrieved memory ids propagate into canonical event memory_refs"
      pattern: "memory_refs"
---

<objective>
Replace the Phase 1 memory stub with real scoped retrieval and wire the resulting memory ids cleanly into the interpretation audit trail.

Purpose: Phase 5 is not real until interpretation can retrieve relevant memory records and later replay exactly which memory ids influenced an event.

Output: scope-aware retrieval works behind the existing async seam, interpretation stays explicit about retrieved memory, and canonical events preserve concrete `memory_refs`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-memory-correction-loop/05-CONTEXT.md
@.planning/phases/05-memory-correction-loop/05-RESEARCH.md
@.planning/phases/05-memory-correction-loop/05-PATTERNS.md
@src/sva/memory/retriever.py
@src/sva/interpret/prompt.py
@src/sva/interpret/adapters/claude.py
@src/sva/models.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Implement scoped tag-first retrieval behind the fixed MemoryRetriever seam</name>
  <files>src/sva/memory/retriever.py, tests/test_memory_retriever.py</files>
  <read_first>
    - src/sva/memory/retriever.py
    - .planning/phases/05-memory-correction-loop/05-CONTEXT.md
    - .planning/phases/05-memory-correction-loop/05-RESEARCH.md
    - tests/test_memory_retriever.py
  </read_first>
  <action>
Replace the stub body with real retrieval behavior while preserving the public signature exactly. Scope candidates to `global ∪ coach:<current>` when a coach is present, narrow by event-type and optional tags first, and then apply bounded ranking/ordering so the returned list stays within the query budget. If semantic ranking is unavailable for a candidate set, degrade honestly to deterministic filtering rather than pretending vector semantics exist.
  </action>
  <acceptance_criteria>
    - retrieval no longer always returns `[]`
    - the public `MemoryRetriever.retrieve(...)` signature remains unchanged
    - results respect scope, tag filtering, and query budget
    - `tests/test_memory_retriever.py` covers retrieval behavior and keeps the existing signature lock
    - `./.venv/bin/pytest tests/test_memory_retriever.py -q` passes
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Preserve explicit prompt composition and event-level memory auditability</name>
  <files>src/sva/interpret/prompt.py, src/sva/interpret/adapters/claude.py, tests/test_interpret_adapter.py</files>
  <read_first>
    - src/sva/interpret/prompt.py
    - src/sva/interpret/adapters/claude.py
    - .planning/phases/05-memory-correction-loop/05-CONTEXT.md
    - .planning/phases/04-interpretation-event-taxonomy/04-02-SUMMARY.md
  </read_first>
  <action>
Keep retrieved memory as an explicit prompt section and ensure canonical event rows preserve the ids of the memory records actually retrieved for that call. If the model omits or underfills `memory_refs`, normalize them from the retrieved memory list so replay and future correction debugging remain possible.
  </action>
  <acceptance_criteria>
    - prompt composition still keeps memory explicit rather than hidden in adapter state
    - event rows preserve concrete retrieved memory ids on the canonical audit trail
    - interpret adapter tests cover memory-ref propagation and compatibility with empty retrieval
    - `./.venv/bin/pytest tests/test_memory_retriever.py tests/test_interpret_adapter.py -q` passes
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_memory_retriever.py tests/test_interpret_adapter.py -q`
- `./.venv/bin/python -m py_compile src/sva/memory/retriever.py src/sva/interpret/prompt.py src/sva/interpret/adapters/claude.py`
</verification>
