---
phase: 05-memory-correction-loop
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/sva/memory/writer.py
  - src/sva/memory/__init__.py
  - tests/test_memory_writer.py
  - tests/test_memory_retriever.py
autonomous: true
requirements:
  - MEMORY-03
  - MEMORY-04
  - MEMORY-05
must_haves:
  truths:
    - "Coach corrections become coach-scoped memory records by default rather than mutating global behavior directly."
    - "Promotion to global is hard-blocked unless corroborated by two distinct coaches and explicit builder curation."
    - "Correction-to-memory derivation remains append-only enough to preserve replayability and auditability."
    - "Single-coach loops cannot silently poison global memory."
  artifacts:
    - path: "src/sva/memory/writer.py"
      provides: "correction-to-memory derivation and promotion gating logic"
    - path: "tests/test_memory_writer.py"
      provides: "explicit contamination-control coverage for promotion decisions"
  key_links:
    - from: "src/sva/memory/writer.py"
      to: "src/sva/memory/corrections_dao.py"
      via: "writer derives memory rows from immutable correction provenance"
      pattern: "correction_id"
    - from: "src/sva/memory/writer.py"
      to: "src/sva/memory/records_dao.py"
      via: "promotion gate controls whether records stay coach-scoped or can become global"
      pattern: "scope"
---

<objective>
Implement the real correction writer and promotion gate so the memory loop compounds accuracy without allowing one coach's conventions to leak globally.

Purpose: Phase 5 only earns its keep if coach corrections can produce memory records safely, with replayable provenance and code-enforced contamination controls.

Output: correction-derived memory writing exists, default scope is coach-specific, and global promotion is protected by explicit corroboration + curation rules.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-memory-correction-loop/05-CONTEXT.md
@.planning/phases/05-memory-correction-loop/05-RESEARCH.md
@.planning/phases/05-memory-correction-loop/05-PATTERNS.md
@src/sva/models.py
@src/sva/memory/records_dao.py
@src/sva/memory/corrections_dao.py
@src/sva/memory/retriever.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Derive coach-scoped memory records from immutable corrections</name>
  <files>src/sva/memory/writer.py, src/sva/memory/__init__.py, tests/test_memory_writer.py</files>
  <read_first>
    - src/sva/models.py
    - src/sva/memory/records_dao.py
    - src/sva/memory/corrections_dao.py
    - .planning/phases/05-memory-correction-loop/05-CONTEXT.md
    - .planning/phases/05-memory-correction-loop/05-RESEARCH.md
  </read_first>
  <action>
Implement the internal write path that turns a correction row plus its source event provenance into one or more candidate memory records. Default those outputs to `scope="coach:<id>"` and preserve the origin metadata needed for later replay and analysis.
  </action>
  <acceptance_criteria>
    - correction-derived memory rows are created in coach scope by default
    - derived memory records preserve correction/source provenance
    - memory writing logic is isolated from HTTP/API concerns
    - `tests/test_memory_writer.py` covers the default coach-scope path
    - `./.venv/bin/pytest tests/test_memory_writer.py -q` passes
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Enforce the multi-coach promotion gate in code</name>
  <files>src/sva/memory/writer.py, tests/test_memory_writer.py, tests/test_memory_retriever.py</files>
  <read_first>
    - .planning/research/PITFALLS.md
    - .planning/research/SUMMARY.md
    - src/sva/memory/writer.py
  </read_first>
  <action>
Add explicit promotion-gate logic so no record becomes globally applied memory unless the corroboration requirement and builder-curation requirement are both satisfied. Favor small deterministic functions over implicit DAO side effects, and prove with tests that a single coach cannot promote their own convention into global memory.
  </action>
  <acceptance_criteria>
    - promotion to global is code-enforced, not policy-only
    - at least two distinct coach ids are required before global promotion can pass
    - builder curation remains mandatory during alpha
    - tests prove a single-coach feedback loop cannot reach global scope
    - `./.venv/bin/pytest tests/test_memory_writer.py tests/test_memory_retriever.py -q` passes
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_memory_writer.py tests/test_memory_retriever.py -q`
- `./.venv/bin/python -m py_compile src/sva/memory/writer.py src/sva/memory/__init__.py`
</verification>
