---
phase: 05-memory-correction-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sva/models.py
  - src/sva/memory/__init__.py
  - src/sva/memory/records_dao.py
  - src/sva/memory/corrections_dao.py
  - migrations/versions/0007_phase5_memory_and_corrections.py
  - tests/test_models.py
  - tests/test_db_migration.py
  - tests/test_memory_records_dao.py
  - tests/test_corrections_dao.py
autonomous: true
requirements:
  - MEMORY-01
  - MEMORY-03
  - MEMORY-05
must_haves:
  truths:
    - "The repo has first-class persisted memory records and immutable correction rows rather than a retrieval stub with nowhere durable to read or write."
    - "Memory persistence remains model-agnostic and preserves correction provenance without coupling rows to a specific LLM/VLM."
    - "Correction provenance captures source event identity plus enough payload snapshot data for future replay and audit."
    - "Phase 5 extends the existing Postgres/Alembic architecture rather than introducing a second primary memory store."
  artifacts:
    - path: "src/sva/memory/records_dao.py"
      provides: "canonical memory record persistence helpers"
    - path: "src/sva/memory/corrections_dao.py"
      provides: "immutable correction persistence helpers"
    - path: "migrations/versions/0007_phase5_memory_and_corrections.py"
      provides: "memory and correction persistence substrate"
    - path: "tests/test_memory_records_dao.py"
      provides: "DB-gated coverage for canonical memory persistence"
  key_links:
    - from: "src/sva/models.py"
      to: "src/sva/memory/records_dao.py"
      via: "DAO persists canonical MemoryRecord fields rather than provider payloads"
      pattern: "MemoryRecord"
    - from: "src/sva/memory/corrections_dao.py"
      to: "src/sva/events_dao.py"
      via: "corrections reference canonical source event ids and event payload snapshots"
      pattern: "event_id"
    - from: "migrations/versions/0007_phase5_memory_and_corrections.py"
      to: "tests/test_db_migration.py"
      via: "migration surface is asserted in the DB smoke suite"
      pattern: "memory_records"
---

<objective>
Land the Phase 5 persistence substrate: canonical memory record storage, immutable correction rows with provenance, and the migration/test coverage that makes later retrieval and promotion logic safe to build on.

Purpose: retrieval and promotion logic are meaningless if the repo still has nowhere durable and auditable to store memory rows or coach corrections.

Output: the repo has `memory_records` and `corrections` persistence surfaces, canonical DAO helpers, and DB-gated coverage for the new substrate.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-memory-correction-loop/05-CONTEXT.md
@.planning/phases/05-memory-correction-loop/05-RESEARCH.md
@.planning/phases/05-memory-correction-loop/05-PATTERNS.md
@src/sva/models.py
@src/sva/memory/retriever.py
@src/sva/events_dao.py
@src/sva/db.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add canonical memory and correction persistence models</name>
  <files>src/sva/models.py, src/sva/memory/records_dao.py, src/sva/memory/corrections_dao.py, src/sva/memory/__init__.py, tests/test_models.py</files>
  <read_first>
    - src/sva/models.py
    - src/sva/memory/retriever.py
    - src/sva/events_dao.py
    - .planning/phases/05-memory-correction-loop/05-CONTEXT.md
    - .planning/phases/05-memory-correction-loop/05-RESEARCH.md
  </read_first>
  <action>
Introduce any missing canonical Phase 5 model(s) needed for correction provenance, then add narrow DAO helpers for `MemoryRecord` and immutable correction rows. Persist source event snapshots, active memory refs, coach identity, and correction type/proposed payload without mutating canonical event rows in place. Keep the memory package exports small and consistent with the repo’s existing DAO conventions.
  </action>
  <acceptance_criteria>
    - canonical persistence exists for memory rows and corrections
    - correction rows preserve source-event provenance and replay-relevant snapshot data
    - memory persistence remains model-agnostic and provider-neutral
    - `tests/test_models.py` covers any new canonical model contract
    - `./.venv/bin/pytest tests/test_models.py -q` passes
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Add the Postgres migration and DB-gated substrate coverage</name>
  <files>migrations/versions/0007_phase5_memory_and_corrections.py, tests/test_db_migration.py, tests/test_memory_records_dao.py, tests/test_corrections_dao.py</files>
  <read_first>
    - migrations/versions/0005_phase3_observations.py
    - tests/test_db_migration.py
    - tests/test_events_dao.py
    - .planning/phases/05-memory-correction-loop/05-PATTERNS.md
  </read_first>
  <action>
Create a single migration for the initial memory/corrections substrate and add focused DB-gated tests that prove the new tables and canonical DAO operations work when Postgres is available. Favor JSONB snapshots and practical indexes over over-normalizing the first cut. The migration should extend the current Postgres architecture cleanly without requiring a second datastore.
  </action>
  <acceptance_criteria>
    - Postgres schema now includes first-class memory and correction tables
    - DB-gated tests cover insertion/query basics for memory rows and correction rows
    - migration smoke coverage asserts the new schema surface
    - `./.venv/bin/pytest tests/test_memory_records_dao.py tests/test_corrections_dao.py tests/test_db_migration.py -q` passes or skips only when Postgres is unavailable
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_models.py tests/test_memory_records_dao.py tests/test_corrections_dao.py tests/test_db_migration.py -q`
- `./.venv/bin/python -m py_compile src/sva/models.py src/sva/memory/__init__.py src/sva/memory/records_dao.py src/sva/memory/corrections_dao.py migrations/versions/0007_phase5_memory_and_corrections.py`
</verification>
