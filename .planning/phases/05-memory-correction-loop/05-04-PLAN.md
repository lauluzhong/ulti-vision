---
phase: 05-memory-correction-loop
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
gap_closure: true
files_modified:
  - src/sva/memory/retriever.py
  - src/sva/memory/records_dao.py
  - src/sva/memory/embeddings.py
  - migrations/versions/0008_phase5_memory_embeddings.py
  - tests/test_memory_retriever.py
  - tests/test_db_migration.py
autonomous: true
requirements:
  - MEMORY-02
must_haves:
  truths:
    - "Memory retrieval supports real semantic ranking behind a model-agnostic seam rather than deterministic tag-only fallback."
    - "Embedding-provider concerns stay isolated to the memory package and do not leak into Event or MemoryRecord contracts."
    - "The repo can re-rank memory candidates semantically without introducing a second primary datastore."
  artifacts:
    - path: "src/sva/memory/embeddings.py"
      provides: "embedding provider seam for retrieval ranking"
    - path: "src/sva/memory/retriever.py"
      provides: "tag-first plus semantic-ranking retrieval"
    - path: "migrations/versions/0008_phase5_memory_embeddings.py"
      provides: "schema support for persisted embedding/ranking state if needed"
  key_links:
    - from: "src/sva/memory/embeddings.py"
      to: "src/sva/memory/retriever.py"
      via: "retriever ranks candidate memory rows semantically through an isolated provider seam"
      pattern: "embed"
    - from: "src/sva/memory/retriever.py"
      to: "src/sva/interpret/prompt.py"
      via: "semantic ranking improves which bounded memory records reach interpret"
      pattern: "Retrieved memory records"
---

<objective>
Close the remaining Phase 5 gap by adding real semantic-ranking support to retrieval while preserving the repo’s Postgres-first, model-agnostic memory architecture.

Purpose: `MEMORY-02` is not honestly satisfied until retrieval is queryable by semantic similarity as well as by event-type tag.

Output: tag-first candidate narrowing is followed by real semantic ranking behind an embedding-provider seam, with tests proving retrieval behavior beyond deterministic fallback.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-memory-correction-loop/05-CONTEXT.md
@.planning/phases/05-memory-correction-loop/05-RESEARCH.md
@.planning/phases/05-memory-correction-loop/05-PATTERNS.md
@.planning/phases/05-memory-correction-loop/05-02-SUMMARY.md
@src/sva/memory/retriever.py
@src/sva/memory/records_dao.py
@src/sva/models.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add an embedding-provider seam for memory ranking</name>
  <files>src/sva/memory/embeddings.py, src/sva/memory/retriever.py, tests/test_memory_retriever.py</files>
  <read_first>
    - src/sva/memory/retriever.py
    - .planning/phases/05-memory-correction-loop/05-RESEARCH.md
    - .planning/research/SUMMARY.md
  </read_first>
  <action>
Introduce a model-agnostic embedding/ranking seam inside the memory package so retrieval can compare query context against candidate memory rows semantically without leaking provider specifics into the rest of the repo. Keep the public retriever signature unchanged and prefer explicit dependency seams over hidden globals.
  </action>
  <acceptance_criteria>
    - semantic ranking exists behind the memory seam
    - the public retriever signature stays unchanged
    - retrieval tests prove semantic ranking influences bounded result selection
    - `./.venv/bin/pytest tests/test_memory_retriever.py -q` passes
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Persist or surface enough embedding state to support repeatable ranking</name>
  <files>src/sva/memory/records_dao.py, migrations/versions/0008_phase5_memory_embeddings.py, tests/test_db_migration.py</files>
  <read_first>
    - src/sva/memory/records_dao.py
    - migrations/versions/0007_phase5_memory_and_corrections.py
    - tests/test_db_migration.py
  </read_first>
  <action>
Add the minimal schema/storage support needed for repeatable semantic retrieval in this repo’s Postgres-first architecture. Keep the change as small as possible and avoid introducing a second primary store just for ranking.
  </action>
  <acceptance_criteria>
    - the schema supports the chosen semantic-ranking path
    - DB migration coverage reflects the new support surface
    - `./.venv/bin/pytest tests/test_db_migration.py -q` passes or skips only when Postgres is unavailable
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_memory_retriever.py tests/test_db_migration.py -q`
- `./.venv/bin/python -m py_compile src/sva/memory/retriever.py src/sva/memory/embeddings.py`
</verification>
