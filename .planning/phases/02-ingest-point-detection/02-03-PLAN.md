---
phase: 02-ingest-point-detection
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/sva/models.py
  - src/sva/events_dao.py
  - src/sva/pipeline.py
  - src/sva/interpret/adapters/claude.py
  - migrations/versions/0004_phase2_point_scoped_events.py
  - tests/test_point_scoped_pipeline.py
  - tests/test_events_dao.py
  - tests/test_cli_e2e.py
autonomous: true
requirements:
  - POINT-03
must_haves:
  truths:
    - "Every persisted event after Phase 2 carries a non-null `point_id` and an explicit in-point timestamp."
    - "The pipeline runs point detection before per-window perception and assigns windows/events through the persisted point rows rather than recomputing point membership ad hoc."
    - "Point filtering is a first-class query path: `WHERE point_id = ?` returns correctly scoped rows."
    - "Phase 1's absolute timestamp contract remains intact; in-point timestamps augment rather than replace `video_ts_ms`."
  artifacts:
    - path: "src/sva/models.py"
      provides: "Point-aware event contract"
    - path: "src/sva/pipeline.py"
      provides: "Point-aware pipeline orchestration"
    - path: "migrations/versions/0004_phase2_point_scoped_events.py"
      provides: "event schema tightening for point-scoped persistence"
  key_links:
    - from: "src/sva/pipeline.py"
      to: "src/sva/points/dao.py"
      via: "pipeline resolves point membership before interpret/persist"
      pattern: "point"
    - from: "src/sva/events_dao.py"
      to: "src/sva/models.py"
      via: "DAO persists non-null point contract"
      pattern: "point_id"
---

<objective>
Propagate Phase 2's point boundary results through the event contract and pipeline so every later event is point-scoped by construction, not by best-effort backfilling.

Purpose: This plan closes the loop on Phase 2. It converts point detection from a side artifact into the authoritative upstream contract that all later perception, interpretation, queries, and exports depend on.

Output: Running the pipeline on a point-detected ingest produces events with non-null `point_id`, explicit in-point timestamps, and queryable point-scoped persistence semantics.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingest-point-detection/02-CONTEXT.md
@.planning/phases/02-ingest-point-detection/02-RESEARCH.md
@src/sva/models.py
@src/sva/pipeline.py
@src/sva/events_dao.py
@src/sva/interpret/adapters/claude.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Tighten the event contract and persistence path for point-scoped events</name>
  <files>src/sva/models.py, src/sva/events_dao.py, src/sva/interpret/adapters/claude.py, migrations/versions/0004_phase2_point_scoped_events.py, tests/test_events_dao.py</files>
  <read_first>
    - src/sva/models.py
    - src/sva/events_dao.py
    - src/sva/interpret/adapters/claude.py
    - .planning/phases/02-ingest-point-detection/02-CONTEXT.md
    - .planning/phases/02-ingest-point-detection/02-RESEARCH.md
  </read_first>
  <action>
Update the `Event` contract so Phase 2 has an explicit in-point timestamp field and a non-null point-assignment expectation. Add the necessary migration to persist the in-point timestamp column and tighten point-scoped event persistence. Update the Phase 1 Claude stub adapter and DAO path so generated events carry the point metadata required by the new contract rather than leaving `point_id` null.
  </action>
  <acceptance_criteria>
    - `src/sva/models.py` defines an explicit in-point timestamp field on `Event`
    - `src/sva/events_dao.py` persists that in-point timestamp field
    - `migrations/versions/0004_phase2_point_scoped_events.py` updates the events schema for the new point-scoped contract
    - `tests/test_events_dao.py` verifies a persisted event round-trips with non-null `point_id` and in-point timestamp
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Insert point detection into the pipeline and verify point-scoped end-to-end behavior</name>
  <files>src/sva/pipeline.py, tests/test_point_scoped_pipeline.py, tests/test_cli_e2e.py</files>
  <read_first>
    - src/sva/pipeline.py
    - src/sva/ingest/ingest.py
    - src/sva/points/detector.py
    - src/sva/points/dao.py
    - .planning/ROADMAP.md
    - .planning/phases/02-ingest-point-detection/02-CONTEXT.md
  </read_first>
  <action>
Update `src/sva/pipeline.py` so point detection runs immediately after ingest normalization and before any per-window perception or interpretation. Assign windows to persisted point rows, propagate `point_id` and point ordinal into trace and event contexts, derive in-point timestamps from the owning point's start time, and ensure persisted events can be queried by `point_id` as a first-class path. Update the end-to-end and pipeline tests so Phase 2 proves the new execution order and point-scoped persistence behavior.
  </action>
  <acceptance_criteria>
    - `src/sva/pipeline.py` invokes point detection before any `run_window(...)` perception loop
    - `tests/test_point_scoped_pipeline.py` verifies the execution order ingest -> point detect -> perceive -> interpret -> persist
    - `tests/test_point_scoped_pipeline.py` or `tests/test_cli_e2e.py` verifies persisted events have non-null `point_id`
    - at least one test executes a `WHERE point_id = ?` query path and proves it returns only the target point's events
  </acceptance_criteria>
</task>

</tasks>
