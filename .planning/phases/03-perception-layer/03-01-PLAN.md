---
phase: 03-perception-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sva/models.py
  - src/sva/observations_dao.py
  - migrations/versions/0005_phase3_observations.py
  - tests/test_models.py
  - tests/test_observations_dao.py
  - tests/test_db_migration.py
autonomous: true
requirements:
  - PERCEIVE-02
  - PERCEIVE-03
must_haves:
  truths:
    - "Observation output is a durable, queryable intermediate artifact rather than an in-memory handoff."
    - "The canonical Observation contract expresses disc ambiguity and scene uncertainty without leaking Gemini-only fields."
    - "Cache identity is the exact `(video_id, window_id, prompt_version_hash)` triple and is enforced from Postgres-backed persistence."
    - "Cached observations survive process restarts and can be reused by later pipeline stages."
  artifacts:
    - path: "src/sva/models.py"
      provides: "versioned model-agnostic Observation schema with structured ambiguity fields"
    - path: "src/sva/observations_dao.py"
      provides: "observation row persistence and exact-triple cache lookup helpers"
    - path: "migrations/versions/0005_phase3_observations.py"
      provides: "observations table and cache-key indexes"
  key_links:
    - from: "src/sva/models.py"
      to: "src/sva/observations_dao.py"
      via: "DAO serializes and rehydrates canonical Observation payloads"
      pattern: "Observation"
    - from: "src/sva/observations_dao.py"
      to: "migrations/versions/0005_phase3_observations.py"
      via: "DAO cache lookup matches persisted `(video_id, window_id, prompt_version_hash)` columns"
      pattern: "prompt_version_hash"
---

<objective>
Create the perception-layer storage backbone: a refined but still swap-safe `Observation` contract, a first-class `observations` table, and a narrow DAO that makes persisted observation reuse a concrete Phase 3 capability instead of a future optimization.

Purpose: Phase 3 starts paying real VLM cost. This plan lands the persisted observation and cache-key contract first so later Gemini and runner work can plug into a durable, testable backbone instead of inventing storage ad hoc.

Output: The repo has a Postgres-backed observations persistence path keyed on `(video_id, window_id, prompt_version_hash)` and typed tests proving canonical `Observation` rows round-trip cleanly. Cache lookup ownership stays deferred to `run_window(...)` in `03-03`; this plan only creates the storage contract that path depends on.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-perception-layer/03-CONTEXT.md
@.planning/phases/03-perception-layer/03-RESEARCH.md
@.planning/phases/03-perception-layer/03-PATTERNS.md
@src/sva/models.py
@src/sva/db.py
@src/sva/points/dao.py
@migrations/versions/0003_phase2_points.py
@migrations/versions/0004_phase2_point_scoped_events.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Refine the canonical Observation schema and create the observations table contract</name>
  <files>src/sva/models.py, migrations/versions/0005_phase3_observations.py, tests/test_models.py</files>
  <read_first>
    - src/sva/models.py
    - migrations/versions/0003_phase2_points.py
    - migrations/versions/0004_phase2_point_scoped_events.py
    - .planning/phases/03-perception-layer/03-CONTEXT.md
    - .planning/phases/03-perception-layer/03-RESEARCH.md
    - .planning/research/PITFALLS.md
  </read_first>
  <action>
Update `Observation` in `src/sva/models.py` per D-04 so downstream code still consumes one model-agnostic contract, but extend the schema where research already identified blind spots per D-05 and D-06. Add structured ambiguity fields such as an explicit disc-visibility quality enum and a scene-level multiple-disc flag, keep defaults conservative, and preserve swap-safety by forbidding provider-specific field leakage. Then add `migrations/versions/0005_phase3_observations.py` creating a first-class `observations` table per D-07, D-08, and D-09 with concrete columns for `observation_id`, `game_id`, `video_id`, `point_id`, `point_ordinal`, `window_id`, `prompt_version_hash`, observation/video timestamps, canonical payload JSONB, `schema_version`, `raw_response_ref`, and timestamps. Add indexes that make exact-triple cache lookups and point-scoped replay cheap, and update `tests/test_models.py` so the new fields are locked by JSON round-trip and safe-default assertions.
  </action>
  <acceptance_criteria>
    - `src/sva/models.py` keeps `Observation` model-agnostic while adding structured ambiguity fields instead of relying on free-form prose alone
    - `migrations/versions/0005_phase3_observations.py` creates an `observations` table with explicit `video_id`, `window_id`, and `prompt_version_hash` columns and an index covering that triple
    - provider-specific payload shape remains outside canonical columns; only `raw_response_ref` is persisted as the escape hatch
    - `tests/test_models.py` proves the refined Observation schema still round-trips and defaults uncertainty fields safely
    - `./.venv/bin/pytest tests/test_models.py -q` passes
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Add the observations DAO and DB-gated cache-key persistence coverage</name>
  <files>src/sva/observations_dao.py, tests/test_observations_dao.py, tests/test_db_migration.py</files>
  <read_first>
    - src/sva/points/dao.py
    - src/sva/events_dao.py
    - src/sva/db.py
    - .planning/phases/03-perception-layer/03-PATTERNS.md
    - .planning/phases/03-perception-layer/03-CONTEXT.md
  </read_first>
  <action>
Create `src/sva/observations_dao.py` following the repo’s narrow-DAO pattern. The DAO should persist typed `Observation` rows, expose an exact-triple cache lookup helper keyed on `(video_id, window_id, prompt_version_hash)` per D-08, and return canonical `Observation` instances ready for downstream interpretation per D-15. Keep the storage contract restart-safe per D-07 by writing rows in Postgres, not ephemeral files, and include point metadata so Phase 2’s persisted point rows remain the authoritative grouping primitive later. Add DB-gated tests in `tests/test_observations_dao.py` proving insert + cache-hit rehydration work from the real database, and extend `tests/test_db_migration.py` so the migrated schema asserts `observations` exists at head.
  </action>
  <acceptance_criteria>
    - `src/sva/observations_dao.py` defines focused helpers for insert and exact-triple cache lookup
    - cache lookup returns typed `Observation` objects rather than raw dicts or provider payloads
    - `tests/test_observations_dao.py` verifies rows can be inserted and retrieved by `(video_id, window_id, prompt_version_hash)`
    - `tests/test_db_migration.py` asserts the `observations` table exists after `alembic upgrade head`
    - `./.venv/bin/pytest tests/test_observations_dao.py tests/test_db_migration.py -q` passes or skips only when Postgres is unreachable
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_models.py -q`
- `./.venv/bin/pytest tests/test_observations_dao.py tests/test_db_migration.py -q`
- `./.venv/bin/python -m py_compile src/sva/models.py src/sva/observations_dao.py migrations/versions/0005_phase3_observations.py`
</verification>
