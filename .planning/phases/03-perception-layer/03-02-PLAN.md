---
phase: 03-perception-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sva/perceive/adapters/gemini.py
  - src/sva/observability/langfuse.py
  - src/sva/observability/cost.py
  - tests/test_perceive_adapter.py
autonomous: true
requirements:
  - PERCEIVE-02
  - PERCEIVE-04
must_haves:
  truths:
    - "Gemini 2.5 Flash is the real default perceiver and still emits canonical Observation output through the swap-safe interface."
    - "Prompt hash, latency, retries, cost, and point/window context are visible on every real Gemini perception call."
    - "Retryable Gemini rate limits back off with a cap and fail explicitly after exhaustion."
    - "Canonical Observation parsing stays conservative when the model is uncertain."
  artifacts:
    - path: "src/sva/perceive/adapters/gemini.py"
      provides: "real Gemini native-video perceiver with schema-safe Observation parsing"
    - path: "src/sva/observability/langfuse.py"
      provides: "per-call trace metadata for prompt hash, latency, retries, and terminal status"
    - path: "tests/test_perceive_adapter.py"
      provides: "adapter parsing and retry coverage"
  key_links:
    - from: "src/sva/perceive/adapters/gemini.py"
      to: "src/sva/observability/langfuse.py"
      via: "Gemini call uses TraceContext and observe_call metadata for prompt-hash observability"
      pattern: "prompt_version_hash"
    - from: "src/sva/perceive/adapters/gemini.py"
      to: "src/sva/observability/cost.py"
      via: "adapter records fresh-call Gemini cost from real token counts"
      pattern: "estimate_gemini_cost"
---

<objective>
Replace the Phase 1 Gemini stub with a real Gemini 2.5 Flash perceiver that uses the native video path, emits schema-safe `Observation` objects, and makes retry/cost/latency behavior explicit instead of hidden inside SDK defaults.

Purpose: Phase 3 only works if the expensive VLM boundary is both real and observable. This plan deepens the single swap-point adapter without leaking provider details downstream or weakening the existing observability contract.

Output: `GeminiPerceiver` becomes a real provider adapter with bounded retry behavior, structured Observation parsing, and trace metadata rich enough to debug cost and rate-limit problems at the window level.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-perception-layer/03-CONTEXT.md
@.planning/phases/03-perception-layer/03-RESEARCH.md
@.planning/phases/03-perception-layer/03-PATTERNS.md
@src/sva/models.py
@src/sva/perceive/adapters/base.py
@src/sva/perceive/adapters/gemini.py
@src/sva/observability/langfuse.py
@src/sva/observability/cost.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Implement the real Gemini native-video adapter and schema-safe Observation parsing</name>
  <files>src/sva/perceive/adapters/gemini.py, tests/test_perceive_adapter.py</files>
  <read_first>
    - src/sva/perceive/adapters/gemini.py
    - src/sva/perceive/adapters/base.py
    - src/sva/models.py
    - .planning/phases/03-perception-layer/03-CONTEXT.md
    - .planning/phases/03-perception-layer/03-RESEARCH.md
    - .planning/research/STACK.md
  </read_first>
  <action>
Replace the Phase 1 stub in `src/sva/perceive/adapters/gemini.py` with a real Gemini 2.5 Flash implementation per D-10 and D-11, using Gemini’s native video path instead of local frame extraction. Keep `GeminiPerceiver` behind `make_default_perceiver()` and the existing `PerceiveWindow`/`Observation` seam per D-01 and D-04. Build one canonical prompt/schema contract, compute a stable `prompt_version_hash` from the exact prompt string that later cache code can reuse, and parse provider output into canonical `Observation` instances without leaking provider-specific fields past `raw_response_ref`. Honor D-05 and D-06 by mapping ambiguous visual cases into the new structured uncertainty fields instead of inventing confident facts, and expand `tests/test_perceive_adapter.py` so SDK-mocked responses prove schema-safe parsing for both clear and ambiguous windows.
  </action>
  <acceptance_criteria>
    - `src/sva/perceive/adapters/gemini.py` performs a real Gemini native-video call path rather than returning the Phase 1 stub payload
    - the adapter computes and propagates a stable `prompt_version_hash` from the exact prompt text it sends
    - canonical `Observation` output contains only swap-safe fields, with any provider escape hatch stored via `raw_response_ref`
    - `tests/test_perceive_adapter.py` covers both ordinary parsing and ambiguity-field population from mocked Gemini responses
    - `./.venv/bin/pytest tests/test_perceive_adapter.py tests/test_models.py -q` passes
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Add bounded Gemini retry behavior and richer perception-call observability</name>
  <files>src/sva/perceive/adapters/gemini.py, src/sva/observability/langfuse.py, src/sva/observability/cost.py, tests/test_perceive_adapter.py</files>
  <read_first>
    - src/sva/observability/langfuse.py
    - src/sva/observability/cost.py
    - src/sva/perceive/adapters/gemini.py
    - .planning/phases/03-perception-layer/03-CONTEXT.md
    - .planning/phases/03-perception-layer/03-RESEARCH.md
  </read_first>
  <action>
Extend perception observability per D-12 and D-14 instead of bolting on ad hoc logs. Update `TraceContext`/`observe_call` metadata so real Gemini calls carry `prompt_version_hash`, latency, retry count, and terminal status while preserving the existing non-fatal Langfuse behavior. In `src/sva/perceive/adapters/gemini.py`, add bounded exponential backoff for retryable Gemini failures (especially 429/rate-limit responses) with an explicit maximum-attempt cap and a surfaced error when retries are exhausted per D-12. Update `src/sva/observability/cost.py` only where needed so cost estimation still reflects real Gemini token counts and model ids cleanly. Expand `tests/test_perceive_adapter.py` to prove retry-then-success and retry-exhaustion behavior without swallowing failures.
  </action>
  <acceptance_criteria>
    - `TraceContext` and `observe_call` expose `prompt_version_hash` and latency/retry metadata for perception calls
    - retryable Gemini failures use capped exponential backoff and do not degrade into silent empty observations
    - exhausted retries raise an explicit exception path that the runner can handle later
    - `tests/test_perceive_adapter.py` verifies both retry success and retry exhaustion
    - cache ownership remains outside this plan: the adapter computes and propagates `prompt_version_hash`, but pre-Gemini cache-hit short-circuiting is implemented in `run_window(...)` during `03-03`
    - `./.venv/bin/pytest tests/test_perceive_adapter.py -q` passes
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_perceive_adapter.py tests/test_models.py -q`
- `./.venv/bin/python -m py_compile src/sva/perceive/adapters/gemini.py src/sva/observability/langfuse.py src/sva/observability/cost.py`
</verification>
