---
phase: 03-perception-layer
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/sva/ingest/sampler.py
  - src/sva/perceive/runner.py
  - src/sva/pipeline.py
  - tests/test_sampler.py
  - tests/test_perceive_runner.py
  - tests/test_point_scoped_pipeline.py
  - tests/test_observability.py
autonomous: true
requirements:
  - PERCEIVE-01
  - PERCEIVE-03
  - PERCEIVE-04
must_haves:
  truths:
    - "Sampling stays behind one control surface: default 1 fps, valid only through 3 fps, and the existing PerceiveWindow boundary stays intact."
    - "Cache lookup happens in `run_window(...)` before Gemini invocation, and a cache hit returns persisted Observations without a duplicate VLM call for the same triple."
    - "The point-aware pipeline persists observations before interpretation and can rerun interpretation from cached observations."
    - "Perception continues to honor Phase 2 point ownership instead of rebuilding point membership ad hoc."
    - "Automated verification proves both zero duplicate Gemini calls and preserved perception observability on cache-hit reruns."
  artifacts:
    - path: "src/sva/ingest/sampler.py"
      provides: "validated sampling envelope and deterministic window generation"
    - path: "src/sva/perceive/runner.py"
      provides: "cache-first window execution path"
    - path: "src/sva/pipeline.py"
      provides: "point-aware observation persistence before interpret"
    - path: "tests/test_observability.py"
      provides: "prompt-hash and perception observability proof for cache-aware reruns"
  key_links:
    - from: "src/sva/ingest/sampler.py"
      to: "src/sva/pipeline.py"
      via: "pipeline passes a single fps control surface into sampler-produced windows"
      pattern: "fps"
    - from: "src/sva/perceive/runner.py"
      to: "src/sva/observations_dao.py"
      via: "runner checks persisted exact-triple cache before provider invocation"
      pattern: "prompt_version_hash"
    - from: "src/sva/pipeline.py"
      to: "src/sva/perceive/runner.py"
      via: "pipeline reuses cached observations and only invokes Gemini on cache miss"
      pattern: "run_window"
---

<objective>
Wire the Phase 3 perception layer into the point-aware pipeline: sampler validation, cache-first runner behavior, and observation persistence before interpretation so prompt iteration stops re-paying Gemini cost.

Purpose: The first two plans create the storage and provider halves. This plan joins them at the stage boundary where VLM spend is either controlled correctly or silently explodes during iteration.

Output: The sampler enforces the 1–3 fps envelope through one config seam, the runner short-circuits exact-triple cache hits before Gemini, and pipeline/observability tests prove both zero duplicate Gemini calls and intact prompt-hash/cost metadata on cache-aware reruns.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-perception-layer/03-CONTEXT.md
@.planning/phases/03-perception-layer/03-RESEARCH.md
@.planning/phases/03-perception-layer/03-PATTERNS.md
@.planning/phases/02-ingest-point-detection/02-03-SUMMARY.md
@src/sva/ingest/sampler.py
@src/sva/perceive/runner.py
@src/sva/pipeline.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Enforce the configurable sampling envelope through one sampler control surface</name>
  <files>src/sva/ingest/sampler.py, tests/test_sampler.py</files>
  <read_first>
    - src/sva/ingest/sampler.py
    - src/sva/perceive/adapters/base.py
    - .planning/PROJECT.md
    - .planning/phases/03-perception-layer/03-CONTEXT.md
    - .planning/phases/03-perception-layer/03-RESEARCH.md
  </read_first>
  <action>
Extend the existing sampler per D-01, D-02, and D-03 instead of inventing a new perception-config layer. Make `fps` materially control emitted offsets/windows, default it to `1`, reject invalid values outside the v1 envelope with a clear error, and keep one edit surface that later CLI/API layers can pass through unchanged. Preserve the current `PerceiveWindow` boundary by having sampler output stay window-oriented, and add deterministic window-id generation inputs or helpers where needed so reruns at the same `fps` produce stable cache identities. Add `tests/test_sampler.py` covering `fps` values `1`, `2`, `3`, rejection above `3`, and deterministic window generation.
  </action>
  <acceptance_criteria>
    - `src/sva/ingest/sampler.py` makes `fps` affect sampling instead of ignoring it
    - the default remains `1 fps`, and `fps > 3` fails fast with a clear validation error
    - there is one configuration seam for sampling rather than separate sampler/perceiver/pipeline knobs
    - `tests/test_sampler.py` covers valid envelope behavior and rejection outside the envelope
    - `./.venv/bin/pytest tests/test_sampler.py -q` passes
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Add cache-first runner logic and point-aware pipeline reuse proof</name>
  <files>src/sva/perceive/runner.py, src/sva/pipeline.py, tests/test_perceive_runner.py, tests/test_point_scoped_pipeline.py</files>
  <read_first>
    - src/sva/perceive/runner.py
    - src/sva/pipeline.py
    - src/sva/observations_dao.py
    - .planning/phases/02-ingest-point-detection/02-03-SUMMARY.md
    - .planning/phases/03-perception-layer/03-CONTEXT.md
  </read_first>
  <action>
Update `src/sva/perceive/runner.py` and `src/sva/pipeline.py` so Phase 2’s persisted point rows remain the source of truth per D-13 while Phase 3 adds observation reuse. **Runner ownership is canonical in this plan:** `run_window(...)` must compute or receive the same `prompt_version_hash` emitted by the Gemini adapter, check Postgres for an exact-triple cache hit per D-07 and D-08 before any `perceiver.perceive(...)` call, and return persisted `Observation` objects directly on a hit. The pipeline should call that cache-first runner, persist observations immediately after miss-path perception succeeds, and only then continue toward interpretation per D-15. Keep perception trace continuity intact by passing `game_id`, `video_id`, `window_id`, `point_id`, and `prompt_version_hash` through both hit and miss paths per D-14. Add `tests/test_perceive_runner.py` proving hit vs miss behavior and extend `tests/test_point_scoped_pipeline.py` so the point-aware pipeline proves: detect points -> resolve point-owned windows -> runner cache check -> perceive/persist only on miss -> interpret, with zero duplicate Gemini invocations on a cache hit for the same triple.
  </action>
  <acceptance_criteria>
    - `src/sva/perceive/runner.py` checks persisted cache state before invoking the perceiver
    - cache hits return canonical `Observation` objects and do not invoke Gemini or record fresh provider cost
    - `src/sva/pipeline.py` persists observations before `run_point(...)` and continues to use Phase 2 point rows as the ownership source
    - `tests/test_perceive_runner.py` proves hit/miss branching against the exact `(video_id, window_id, prompt_version_hash)` contract
    - `tests/test_point_scoped_pipeline.py` proves the second run reuses cached observations with zero duplicate VLM calls for the same triple
    - `tests/test_observability.py` proves prompt-version-hash sensitivity remains intact and that cache-aware reruns preserve the perception observability contract rather than bypassing it silently
    - `./.venv/bin/pytest tests/test_perceive_runner.py tests/test_point_scoped_pipeline.py -q` passes
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest tests/test_sampler.py -q`
- `./.venv/bin/pytest tests/test_perceive_runner.py tests/test_point_scoped_pipeline.py tests/test_observability.py -q`
- `./.venv/bin/python -m py_compile src/sva/ingest/sampler.py src/sva/perceive/runner.py src/sva/pipeline.py`
</verification>
